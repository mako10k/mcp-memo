{
  "name": "mcp-memo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lint": "bun run check",
    "check": "echo 'No checks defined yet'",
    "format": "echo 'No formatter configured yet'"
  },
  "devDependencies": {
    "pm2": "^5.3.0"
  },
  "packageManager": "bun@1.1.12"
}
