{
  "name": "@mcp/memory-server",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --config wrangler.toml",
    "build": "wrangler deploy --dry-run --config wrangler.toml",
    "test": "bun test",
    "create:api-key": "bun run src/scripts/createApiKey.ts"
  },
  "dependencies": {
    "@mcp/shared": "workspace:*",
    "@neondatabase/serverless": "^0.10.4",
    "wrangler": "^4.42.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.4.5"
  }
}
